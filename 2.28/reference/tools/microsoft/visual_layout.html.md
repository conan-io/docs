<a id="conan-tools-microsoft-visual-layout"></a>

# vs_layout

### vs_layout(conanfile)

Initialize a layout for a typical Visual Studio project.

* **Parameters:**
  **conanfile** – `< ConanFile object >` The current recipe object. Always use `self`.
